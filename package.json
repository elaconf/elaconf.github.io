{
  "name": "ela-conf",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "scss-lint _sass/*.scss && htmlproofer ./_site && tape test/*.js | tap-min"
  },
  "dependencies": {
    "scsslint": "0.0.3"
  },
  "devDependencies": {
    "js-yaml": "^2.1.3",
    "tap-min": "^1.1.0",
    "tape": "^4.6.0"
  }
}
