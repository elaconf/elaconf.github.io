{
  "name": "ela-conf",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "scss-lint _sass/*.scss && tape test/*.js | tap-min"
  },
  "bin": {
    "update-avatars": "bin/avatars.js"
  },
  "dependencies": {
    "scsslint": "0.0.3"
  },
  "devDependencies": {
    "js-yaml": "^2.1.3",
    "tap-min": "^1.1.0",
    "tape": "^4.6.0"
  }
}
