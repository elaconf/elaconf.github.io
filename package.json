{
  "name": "ela-conf",
  "version": "0.0.0",
  "description": "",
  "scripts": {
    "test": "scss-lint _sass/*.scss && htmlproofer --check-html ./_site && node test/*.js | tap-min"
  },
  "dependencies": {
    "scsslint": "0.0.3"
  },
  "devDependencies": {
    "js-base64": "^2.1.9",
    "js-yaml": "^2.1.3",
    "slugify": "^1.1.0",
    "tap-min": "^1.2.1",
    "tape": "^4.7.0"
  }
}
